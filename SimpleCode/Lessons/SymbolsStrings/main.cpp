#include <iostream>

/*
    Строки и указатели в С++
*/

int main()
{

    char str1[] = "Hello!";
    char str2[]{};

    // const size_t size        = 3;
    // const char* strArr[size] = {"AAA", "BBB", "CCC"};

    // for (size_t i = 0; i < size; ++i)
    // {
    //     std::cout << strArr[i] << std::endl;
    // }

    return 0;
}